import type { Metadata } from "next";

const page = () => {
  return <div>page</div>;
};

export default page;

export const metadata: Metadata = {
  title: "Home - ClavardageDirect",
  description:
    "ClavardageDirect is a real-time chat application that makes conversations simple, fast, and seamless.",
};
